name: flutter_animaciones_notificaciones
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'  # ✅ Dart 3.8.0 como en tu SDK

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  flutter_staggered_animations: ^1.1.1
  provider: ^6.1.1
  intl: ^0.20.2  # ⬆️ compatible con Dart 3.8
  timezone: ^0.10.1
  permission_handler: ^12.0.0+1  # ⬆️ versión reciente
  flutter_local_notifications: ^19.3.0  # ⬆️ último patch

  # Hive para base de datos local
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0  # ⬆️ actualizado para Dart 3.8
  build_runner: ^2.4.8
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
